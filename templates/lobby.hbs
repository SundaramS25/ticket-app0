<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Ticket Booking App</title>
    <link rel="stylesheet" href="/lobbystyle.css" />
    <style>body { margin: 0; padding: 0; font-family: "Jost", sans-serif; /*
      background: #f5efe7; */ background-color: #ffdee9; background-image:
      linear-gradient(0deg, #ffdee9 0%, #b5fffc 100%); /* background-image:
      linear-gradient(to right, #74ebd5 0%, #9face6 100%); */ color: #213555;
      display: flex; flex-direction: column; justify-content: center;
      align-items: center; align-content: center; } #main { display: flex;
      flex-direction: column; justify-content: center; align-items: center;
      height: 100vh; } .card { display: flex; flex-direction: column;
      justify-content: center; align-items: center; /* background-color:
      #d8c4b6; */ background-color: #ffdee9; background-image:
      linear-gradient(0deg, #ffdee9 0%, #b5fffc 100%); box-shadow: 0 4px 8px 0
      rgba(0, 0, 0, 0.2); transition: 0.3s; width: 40%; height: 15%;
      border-radius: 10px; } .card:hover { box-shadow: 0 8px 16px 0 rgba(0, 0,
      0, 0.2); } .head { color: #213555; } button { margin-left: 8px;
      background-color: #4f709c; border: none; color: white; height: 36px;
      font-size: 15px; width: 80px; border-radius: 18px; font-weight: bold;
      cursor: pointer; transition: box-shadow 0.15s; } button:hover {
      box-shadow: 5px 5px 10px rgba(0, 0, 0, 0.15); }
    </style>
  </head>
  <body>
    <div id="main">
      <h1 style="font-size:60px;margin-top:0px">Fly Right Booking</h1>
      <div class="card" style="margin-bottom: 30px;">
        <h4 class="head">User Login</h4>
        <form action="/login">
          <button type="submit">login</button></form>
      </div>
      <div class="card">
        <h4 class="head">Admin Login</h4>
        <form action="/adlogin">
          <button type="submit">login</button></form>
      </div>

    </div>
  </body>
</html>
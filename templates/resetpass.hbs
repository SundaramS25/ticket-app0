<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Reset Password</title>
    <link rel="stylesheet" href="/resetpass.css" />
    <style>body { font-family: "Jost", sans-serif; background-color: #ffdee9;
      background-image: linear-gradient(0deg, #ffdee9 0%, #b5fffc 100%); color:
      #213555; } .maindiv { display: flex; height: 70vh; flex-direction: column;
      justify-content: center; align-items: center; } .card { box-shadow: 0 4px
      8px 0 rgba(0, 0, 0, 0.2); transition: 0.3s; /* background-color: #d8c4b6;
      */ width: 30%; height: 40%; } .card:hover { box-shadow: 0 8px 16px 0
      rgba(0, 0, 0, 0.2); } .container { padding: 2px 16px; display: flex;
      flex-direction: column; align-items: center; justify-content: center;
      height: 25vh; } .formdiv { display: flex; flex-direction: column;
      align-items: center; } input { font-size: 14px; margin-top: 30px; width:
      250px; height: 30px; } button { margin-top: 15px; background-color:
      #4f709c; border: none; color: white; height: 36px; font-size: 15px; width:
      80px; border-radius: 18px; font-weight: bold; cursor: pointer; transition:
      box-shadow 0.15s; } label { color: #213555; font-size: 20px; }
    </style>
  </head>
  <body>
    <div class="maindiv">
      <h1>Change Password</h1>
      <div class="card">
        <div class="container">
          <form action="/passconf" method="post">
            <div class="formdiv">
              <input
                type="text"
                name="email"
                value="{{this.flightData.email}}"
                hidden
              />
              <input type="password" name="newp" placeholder="New Password" />
              <input
                type="password"
                name="conp"
                placeholder="Confirm Password"
              />
              <button type="submit" style="margin-top: 25px;">change</button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
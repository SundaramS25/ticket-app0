<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Forgot Password</title>
    <link rel="stylesheet" href="/fgetpass.css" />
    <style>body { font-family: "Jost", sans-serif; background-color: #ffdee9;
      background-image: linear-gradient(0deg, #ffdee9 0%, #b5fffc 100%); }
      .maindiv { display: flex; height: 70vh; flex-direction: column;
      justify-content: center; align-items: center; } .card { box-shadow: 0 4px
      8px 0 rgba(0, 0, 0, 0.2); transition: 0.3s; /* background-color: #d8c4b6;
      */ width: 30%; height: 40%; } .card:hover { box-shadow: 0 8px 16px 0
      rgba(0, 0, 0, 0.2); } .container { padding: 2px 16px; display: flex;
      flex-direction: column; align-items: center; justify-content: center;
      height: 25vh; } .formdiv { display: flex; flex-direction: column;
      align-items: center; } input { font-size: 18px; margin-top: 30px; width:
      60%; height: 30px; border: none; background-color: #c4eceb; color:
      #213555; border-bottom: 1px solid #4f709c; transition: 0.9s width; }
      input:focus { outline: none; width: 65%; height: 34px; } button {
      margin-top: 15px; background-color: #4f709c; border: none; color: white;
      height: 36px; font-size: 15px; width: 80px; border-radius: 18px;
      font-weight: bold; cursor: pointer; transition: box-shadow 0.15s; } label
      { color: #213555; font-size: 20px; }
    </style>
  </head>
  <body>
    <div class="maindiv">
      <div class="card">
        <div class="container">
          <form action="/otpverify1" method="post">
            <div class="formdiv">
              <label for="">Enter the mail to receive OTP for changing password</label>
              <input type="text" name="emailotp" />
              <button type="submit">verify</button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
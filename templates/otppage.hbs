<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="/otppagestyle.css" />
    <script>
      function limitInput(input) { if (input.value.length > 1) { input.value =
      input.value.slice(0, 1); } } function handleInput(event, nextInput) { if
      (event.target.value.length === 1) { nextInput.focus(); } }
    </script>
    <style>body { font-family: "Jost", sans-serif; /* background: #f5efe7; */
      background-color: #ffdee9; background-image: linear-gradient(0deg, #ffdee9
      0%, #b5fffc 100%); color: #213555; } .maindiv { display: flex; height:
      70vh; flex-direction: column; justify-content: center; align-items:
      center; } .card { box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2); transition:
      0.3s; /* background-color: #d8c4b6; */ width: 30%; height: 40%; }
      .card:hover { box-shadow: 0 8px 16px 0 rgba(0, 0, 0, 0.2); } .container {
      padding: 2px 16px; display: flex; flex-direction: column; align-items:
      center; justify-content: center; height: 25vh; } .formdiv { display: flex;
      flex-direction: column; align-items: center; } input { width: 40px;
      height: 40px; text-align: center; font-size: 22px; } button { margin-top:
      15px; background-color: #4f709c; border: none; color: white; height: 36px;
      font-size: 15px; width: 80px; border-radius: 18px; font-weight: bold;
      cursor: pointer; transition: box-shadow 0.15s; } label { color: #213555; }
    </style>
  </head>
  <body>
    <div class="maindiv">
      <h2>OTP Verification</h2>
      <div class="card">
        <div class="container">
          <form action="/signup" method="post">
            <div class="formdiv">
              <label for="">Fill the One Time Password received in the
                mail.(Note:sometimes the mail may be under spam category.)</label><br
              />
              <input
                type="text"
                name="name"
                value={{this.flightData.name}}
                hidden
              />
              <input
                type="email"
                name="email"
                value={{this.flightData.email}}
                hidden
              />
              <input
                type="password"
                name="password"
                value={{this.flightData.password}}
                hidden
              />
              <input
                type="password"
                name="otp"
                value={{this.flightData.otp}}
                hidden
              />
              <div class="otpdiv">
                <input
                  type="text"
                  maxlength="1"
                  oninput="handleInput(event, sdig)"
                  name="fdig"
                />
                <input
                  type="text"
                  maxlength="1"
                  oninput="handleInput(event, tdig)"
                  name="sdig"
                />
                <input
                  type="text"
                  maxlength="1"
                  oninput="handleInput(event, fodig)"
                  name="tdig"
                />
                <input
                  type="text"
                  maxlength="1"
                  oninput="limitInput(this)"
                  name="fodig"
                />
              </div>
              <button type="submit">verify</button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>